@model IEnumerable<Where2Study.Models.kontinent_tekst>

@{
    ViewBag.Title = "Menu";
}

<h3>@Resources.Resources.BrowseContinents</h3>

@if (@Model.Count() != 0)
{
    <p>
        <div class="container">
            <div class="row">
                <div class="col-lg-4">
                    @Resources.Resources.SelectFrom @Model.Count() @Resources.Resources.continents:
                </div>
            </div>
        </div>
    </p>}
    
<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.tekst, "Countries", new {continent=item.tekst})</li>
    }
</ul>


<!--

<div class="container">
    <div class="row">
        <ul>
            @*@foreach (var item in Model)
            {
                <div class="col-lg-3 col-md-6 col-xs-12">
                    <input type="button" class="btn btn-primary btn-lg" style="margin:0.27em;width:100%;" value=@item.naziv title=item.naziv onclick="location.href='@Url.Action("Cities", "Menu", new { country = item.naziv })    '" />

                    @* <li><button type="button" class="btn btn-primary btn-lg" style="margin:0.27em;width:100%;">@Html.ActionLink(item.naziv, "Cities", new { country = item.naziv })</button></li>*@
                </div>
            }

            </ul>
    </div>
    <br />
</div>

-->
@model Where2Study.Models.stupanj_smjer

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>stupanj_smjer</legend>

        @Html.HiddenFor(model => model.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.id_stupanj)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.id_stupanj)
            @Html.ValidationMessageFor(model => model.id_stupanj)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.id_smjer)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.id_smjer)
            @Html.ValidationMessageFor(model => model.id_smjer)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@*@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}*@

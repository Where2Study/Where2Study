@model IEnumerable<Where2Study.Models.smjer_tekst>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_smjer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_jezik)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tekst)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_smjer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_jezik)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tekst)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
